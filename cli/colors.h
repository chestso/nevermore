/* colors.h - semantic color roles (ported from ditty/cli/colors.h)
 *
 * Values are boba style presets; portty renders them through the
 * coffer SGR pipeline.
 */

#ifndef NM_COLORS_H
#define NM_COLORS_H

/* TODO(phase 3): TuiStyle presets — user text, assistant text,
 * tool-call blocks, errors, model/provider status line. */

#endif // NM_COLORS_H
